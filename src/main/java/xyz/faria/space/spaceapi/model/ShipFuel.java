/*
 * SpaceTraders API
 * SpaceTraders is an open-universe game and learning platform that offers a set of HTTP endpoints to control a fleet of ships and explore a multiplayer universe.  The API is documented using [OpenAPI](https://github.com/SpaceTradersAPI/api-docs). You can send your first request right here in your browser to check the status of the game server.  ```json http {   \"method\": \"GET\",   \"url\": \"https://api.spacetraders.io/v2\", } ```  Unlike a traditional game, SpaceTraders does not have a first-party client or app to play the game. Instead, you can use the API to build your own client, write a script to automate your ships, or try an app built by the community.  We have a [Discord channel](https://discord.com/invite/jh6zurdWk5) where you can share your projects, ask questions, and get help from other players.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: joel@spacetraders.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package xyz.faria.space.spaceapi.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import jakarta.persistence.Embeddable;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import xyz.faria.space.spaceapi.client.JSON;

/**
 * Details of the ship&#39;s fuel tanks including how much fuel was consumed during the last transit
 * or action.
 */
@Embeddable
public class ShipFuel {

    public static final String SERIALIZED_NAME_CURRENT = "current";
    @SerializedName(SERIALIZED_NAME_CURRENT)
    @javax.annotation.Nonnull
    private Integer current;

    public static final String SERIALIZED_NAME_CAPACITY = "capacity";
    @SerializedName(SERIALIZED_NAME_CAPACITY)
    @javax.annotation.Nonnull
    private Integer capacity;

    public static final String SERIALIZED_NAME_CONSUMED = "consumed";
    @SerializedName(SERIALIZED_NAME_CONSUMED)
    @javax.annotation.Nullable
    private ShipFuelConsumed consumed;

    public ShipFuel() {
    }

    public ShipFuel current(@javax.annotation.Nonnull Integer current) {
        this.current = current;
        return this;
    }

    /**
     * The current amount of fuel in the ship&#39;s tanks. minimum: 0
     *
     * @return current
     */
    @javax.annotation.Nonnull
    public Integer getCurrent() {
        return current;
    }

    public void setCurrent(@javax.annotation.Nonnull Integer current) {
        this.current = current;
    }


    public ShipFuel capacity(@javax.annotation.Nonnull Integer capacity) {
        this.capacity = capacity;
        return this;
    }

    /**
     * The maximum amount of fuel the ship&#39;s tanks can hold. minimum: 0
     *
     * @return capacity
     */
    @javax.annotation.Nonnull
    public Integer getCapacity() {
        return capacity;
    }

    public void setCapacity(@javax.annotation.Nonnull Integer capacity) {
        this.capacity = capacity;
    }


    public ShipFuel consumed(@javax.annotation.Nullable ShipFuelConsumed consumed) {
        this.consumed = consumed;
        return this;
    }

    /**
     * Get consumed
     *
     * @return consumed
     */
    @javax.annotation.Nullable
    public ShipFuelConsumed getConsumed() {
        return consumed;
    }

    public void setConsumed(@javax.annotation.Nullable ShipFuelConsumed consumed) {
        this.consumed = consumed;
    }


    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ShipFuel shipFuel = (ShipFuel) o;
        return Objects.equals(this.current, shipFuel.current) &&
            Objects.equals(this.capacity, shipFuel.capacity) &&
            Objects.equals(this.consumed, shipFuel.consumed);
    }

    @Override
    public int hashCode() {
        return Objects.hash(current, capacity, consumed);
    }

    @Override
    public String toString() {
        return String.format("%d / %d", current, capacity);
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces (except the first
     * line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }


    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("current", "capacity", "consumed"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("current", "capacity"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ShipFuel
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ShipFuel.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(java.util.Locale.ROOT,
                    "The required field(s) %s in ShipFuel is not found in the empty JSON string",
                    ShipFuel.openapiRequiredFields));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!ShipFuel.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(java.util.Locale.ROOT,
                    "The field `%s` in the JSON string is not defined in the `ShipFuel` properties. JSON: %s",
                    entry.getKey(), jsonElement));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ShipFuel.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(java.util.Locale.ROOT,
                    "The required field `%s` is not found in the JSON string: %s", requiredField,
                    jsonElement));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the optional field `consumed`
        if (jsonObj.get("consumed") != null && !jsonObj.get("consumed").isJsonNull()) {
            ShipFuelConsumed.validateJsonElement(jsonObj.get("consumed"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {

        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ShipFuel.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ShipFuel' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ShipFuel> thisAdapter
                = gson.getDelegateAdapter(this, TypeToken.get(ShipFuel.class));

            return (TypeAdapter<T>) new TypeAdapter<ShipFuel>() {
                @Override
                public void write(JsonWriter out, ShipFuel value) throws IOException {
                    JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                    elementAdapter.write(out, obj);
                }

                @Override
                public ShipFuel read(JsonReader in) throws IOException {
                    JsonElement jsonElement = elementAdapter.read(in);
                    validateJsonElement(jsonElement);
                    return thisAdapter.fromJsonTree(jsonElement);
                }

            }.nullSafe();
        }
    }

    /**
     * Create an instance of ShipFuel given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ShipFuel
     * @throws IOException if the JSON string is invalid with respect to ShipFuel
     */
    public static ShipFuel fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ShipFuel.class);
    }

    /**
     * Convert an instance of ShipFuel to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

