/*
 * SpaceTraders API
 * SpaceTraders is an open-universe game and learning platform that offers a set of HTTP endpoints to control a fleet of ships and explore a multiplayer universe.  The API is documented using [OpenAPI](https://github.com/SpaceTradersAPI/api-docs). You can send your first request right here in your browser to check the status of the game server.  ```json http {   \"method\": \"GET\",   \"url\": \"https://api.spacetraders.io/v2\", } ```  Unlike a traditional game, SpaceTraders does not have a first-party client or app to play the game. Instead, you can use the API to build your own client, write a script to automate your ships, or try an app built by the community.  We have a [Discord channel](https://discord.com/invite/jh6zurdWk5) where you can share your projects, ask questions, and get help from other players.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: joel@spacetraders.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package xyz.faria.space.spaceapi.model;

import com.google.gson.*;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import xyz.faria.space.spaceapi.client.JSON;

import java.io.IOException;
import java.time.OffsetDateTime;
import java.util.*;

/**
 * The terms to fulfill the contract.
 */

public class ContractTerms {
    public static final String SERIALIZED_NAME_DEADLINE = "deadline";
    @SerializedName(SERIALIZED_NAME_DEADLINE)
    @javax.annotation.Nonnull
    private OffsetDateTime deadline;

    public static final String SERIALIZED_NAME_PAYMENT = "payment";
    @SerializedName(SERIALIZED_NAME_PAYMENT)
    @javax.annotation.Nonnull
    private ContractPayment payment;

    public static final String SERIALIZED_NAME_DELIVER = "deliver";
    @SerializedName(SERIALIZED_NAME_DELIVER)
    @javax.annotation.Nullable
    private List<ContractDeliverGood> deliver = new ArrayList<>();

    public ContractTerms() {
    }

    public ContractTerms deadline(@javax.annotation.Nonnull OffsetDateTime deadline) {
        this.deadline = deadline;
        return this;
    }

    /**
     * The deadline for the contract.
     *
     * @return deadline
     */
    @javax.annotation.Nonnull
    public OffsetDateTime getDeadline() {
        return deadline;
    }

    public void setDeadline(@javax.annotation.Nonnull OffsetDateTime deadline) {
        this.deadline = deadline;
    }


    public ContractTerms payment(@javax.annotation.Nonnull ContractPayment payment) {
        this.payment = payment;
        return this;
    }

    /**
     * Get payment
     *
     * @return payment
     */
    @javax.annotation.Nonnull
    public ContractPayment getPayment() {
        return payment;
    }

    public void setPayment(@javax.annotation.Nonnull ContractPayment payment) {
        this.payment = payment;
    }


    public ContractTerms deliver(@javax.annotation.Nullable List<ContractDeliverGood> deliver) {
        this.deliver = deliver;
        return this;
    }

    public ContractTerms addDeliverItem(ContractDeliverGood deliverItem) {
        if (this.deliver == null) {
            this.deliver = new ArrayList<>();
        }
        this.deliver.add(deliverItem);
        return this;
    }

    /**
     * The cargo that needs to be delivered to fulfill the contract.
     *
     * @return deliver
     */
    @javax.annotation.Nullable
    public List<ContractDeliverGood> getDeliver() {
        return deliver;
    }

    public void setDeliver(@javax.annotation.Nullable List<ContractDeliverGood> deliver) {
        this.deliver = deliver;
    }


    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ContractTerms contractTerms = (ContractTerms) o;
        return Objects.equals(this.deadline, contractTerms.deadline) &&
                Objects.equals(this.payment, contractTerms.payment) &&
                Objects.equals(this.deliver, contractTerms.deliver);
    }

    @Override
    public int hashCode() {
        return Objects.hash(deadline, payment, deliver);
    }

    @Override
    public String toString() {
        String sb = "class ContractTerms {\n" +
                "    deadline: " + toIndentedString(deadline) + "\n" +
                "    payment: " + toIndentedString(payment) + "\n" +
                "    deliver: " + toIndentedString(deliver) + "\n" +
                "}";
        return sb;
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }


    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("deadline", "payment", "deliver"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("deadline", "payment"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ContractTerms
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ContractTerms.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in ContractTerms is not found in the empty JSON string", ContractTerms.openapiRequiredFields));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!ContractTerms.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` in the JSON string is not defined in the `ContractTerms` properties. JSON: %s", entry.getKey(), jsonElement));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ContractTerms.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the required field `payment`
        ContractPayment.validateJsonElement(jsonObj.get("payment"));
        if (jsonObj.get("deliver") != null && !jsonObj.get("deliver").isJsonNull()) {
            JsonArray jsonArraydeliver = jsonObj.getAsJsonArray("deliver");
            if (jsonArraydeliver != null) {
                // ensure the json data is an array
                if (!jsonObj.get("deliver").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `deliver` to be an array in the JSON string but got `%s`", jsonObj.get("deliver").toString()));
                }

                // validate the optional field `deliver` (array)
                for (int i = 0; i < jsonArraydeliver.size(); i++) {
                    ContractDeliverGood.validateJsonElement(jsonArraydeliver.get(i));
                }
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ContractTerms.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ContractTerms' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ContractTerms> thisAdapter
                    = gson.getDelegateAdapter(this, TypeToken.get(ContractTerms.class));

            return (TypeAdapter<T>) new TypeAdapter<ContractTerms>() {
                @Override
                public void write(JsonWriter out, ContractTerms value) throws IOException {
                    JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                    elementAdapter.write(out, obj);
                }

                @Override
                public ContractTerms read(JsonReader in) throws IOException {
                    JsonElement jsonElement = elementAdapter.read(in);
                    validateJsonElement(jsonElement);
                    return thisAdapter.fromJsonTree(jsonElement);
                }

            }.nullSafe();
        }
    }

    /**
     * Create an instance of ContractTerms given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ContractTerms
     * @throws IOException if the JSON string is invalid with respect to ContractTerms
     */
    public static ContractTerms fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ContractTerms.class);
    }

    /**
     * Convert an instance of ContractTerms to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

