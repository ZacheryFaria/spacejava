/*
 * SpaceTraders API
 * SpaceTraders is an open-universe game and learning platform that offers a set of HTTP endpoints to control a fleet of ships and explore a multiplayer universe.  The API is documented using [OpenAPI](https://github.com/SpaceTradersAPI/api-docs). You can send your first request right here in your browser to check the status of the game server.  ```json http {   \"method\": \"GET\",   \"url\": \"https://api.spacetraders.io/v2\", } ```  Unlike a traditional game, SpaceTraders does not have a first-party client or app to play the game. Instead, you can use the API to build your own client, write a script to automate your ships, or try an app built by the community.  We have a [Discord channel](https://discord.com/invite/jh6zurdWk5) where you can share your projects, ask questions, and get help from other players.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: joel@spacetraders.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package xyz.faria.space.spaceapi.model;

import com.google.gson.*;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import xyz.faria.space.spaceapi.client.JSON;

import java.io.IOException;
import java.util.*;

/**
 * The construction details of a waypoint.
 */

public class Construction {
    public static final String SERIALIZED_NAME_SYMBOL = "symbol";
    @SerializedName(SERIALIZED_NAME_SYMBOL)
    @javax.annotation.Nonnull
    private String symbol;

    public static final String SERIALIZED_NAME_MATERIALS = "materials";
    @SerializedName(SERIALIZED_NAME_MATERIALS)
    @javax.annotation.Nonnull
    private List<ConstructionMaterial> materials = new ArrayList<>();

    public static final String SERIALIZED_NAME_IS_COMPLETE = "isComplete";
    @SerializedName(SERIALIZED_NAME_IS_COMPLETE)
    @javax.annotation.Nonnull
    private Boolean isComplete;

    public Construction() {
    }

    public Construction symbol(@javax.annotation.Nonnull String symbol) {
        this.symbol = symbol;
        return this;
    }

    /**
     * The symbol of the waypoint.
     *
     * @return symbol
     */
    @javax.annotation.Nonnull
    public String getSymbol() {
        return symbol;
    }

    public void setSymbol(@javax.annotation.Nonnull String symbol) {
        this.symbol = symbol;
    }


    public Construction materials(@javax.annotation.Nonnull List<ConstructionMaterial> materials) {
        this.materials = materials;
        return this;
    }

    public Construction addMaterialsItem(ConstructionMaterial materialsItem) {
        if (this.materials == null) {
            this.materials = new ArrayList<>();
        }
        this.materials.add(materialsItem);
        return this;
    }

    /**
     * The materials required to construct the waypoint.
     *
     * @return materials
     */
    @javax.annotation.Nonnull
    public List<ConstructionMaterial> getMaterials() {
        return materials;
    }

    public void setMaterials(@javax.annotation.Nonnull List<ConstructionMaterial> materials) {
        this.materials = materials;
    }


    public Construction isComplete(@javax.annotation.Nonnull Boolean isComplete) {
        this.isComplete = isComplete;
        return this;
    }

    /**
     * Whether the waypoint has been constructed.
     *
     * @return isComplete
     */
    @javax.annotation.Nonnull
    public Boolean getIsComplete() {
        return isComplete;
    }

    public void setIsComplete(@javax.annotation.Nonnull Boolean isComplete) {
        this.isComplete = isComplete;
    }


    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        Construction construction = (Construction) o;
        return Objects.equals(this.symbol, construction.symbol) &&
                Objects.equals(this.materials, construction.materials) &&
                Objects.equals(this.isComplete, construction.isComplete);
    }

    @Override
    public int hashCode() {
        return Objects.hash(symbol, materials, isComplete);
    }

    @Override
    public String toString() {
        String sb = "class Construction {\n" +
                "    symbol: " + toIndentedString(symbol) + "\n" +
                "    materials: " + toIndentedString(materials) + "\n" +
                "    isComplete: " + toIndentedString(isComplete) + "\n" +
                "}";
        return sb;
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }


    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("symbol", "materials", "isComplete"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("symbol", "materials", "isComplete"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to Construction
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!Construction.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in Construction is not found in the empty JSON string", Construction.openapiRequiredFields));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!Construction.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` in the JSON string is not defined in the `Construction` properties. JSON: %s", entry.getKey(), jsonElement));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : Construction.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("symbol").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `symbol` to be a primitive type in the JSON string but got `%s`", jsonObj.get("symbol").toString()));
        }
        // ensure the json data is an array
        if (!jsonObj.get("materials").isJsonArray()) {
            throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `materials` to be an array in the JSON string but got `%s`", jsonObj.get("materials").toString()));
        }

        JsonArray jsonArraymaterials = jsonObj.getAsJsonArray("materials");
        // validate the required field `materials` (array)
        for (int i = 0; i < jsonArraymaterials.size(); i++) {
            ConstructionMaterial.validateJsonElement(jsonArraymaterials.get(i));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!Construction.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'Construction' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<Construction> thisAdapter
                    = gson.getDelegateAdapter(this, TypeToken.get(Construction.class));

            return (TypeAdapter<T>) new TypeAdapter<Construction>() {
                @Override
                public void write(JsonWriter out, Construction value) throws IOException {
                    JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                    elementAdapter.write(out, obj);
                }

                @Override
                public Construction read(JsonReader in) throws IOException {
                    JsonElement jsonElement = elementAdapter.read(in);
                    validateJsonElement(jsonElement);
                    return thisAdapter.fromJsonTree(jsonElement);
                }

            }.nullSafe();
        }
    }

    /**
     * Create an instance of Construction given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of Construction
     * @throws IOException if the JSON string is invalid with respect to Construction
     */
    public static Construction fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, Construction.class);
    }

    /**
     * Convert an instance of Construction to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

